import React from 'react';
import './Singer.css';

const Singer = (props) => {
    // const { name, age, country, profession, worth, img } = props.singer;
    // console.log(props);


    return (
        <div className="singer">
            {/* <div className="singer-img">

                <img src={img} alt="" />

            </div>

            <div className="singer-data">
                <h4>{name}</h4>
                <p>Age :{age}</p>
                <p>Profession:{profession}</p>
                <p>Worth: {worth}</p>
                <p>Country:{country}</p>
                <button onClick={() => props.addToCart(props.singer)}>addToCart</button>



            </div> */}

        </div>
    );
};

export default Singer;